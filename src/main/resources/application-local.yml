spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/quant_db
    username: postgres
    password: postgres

  data:
    redis:
      host: localhost
      port: 6379

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
